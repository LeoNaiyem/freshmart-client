import React from 'react';
import Header from '../../Shared/Header/Header';

const Orders = () => {
    return (
        <div>
            <Header></Header>
            <h1>Orders Page</h1>
        </div>
    );
};

export default Orders;